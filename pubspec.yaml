name: app_failure
description: Declarative failure handling for Dart and Flutter apps with layered cause chains and processing intent.
version: 1.0.1

repository: https://github.com/konstantinvoronov/app_failure
issue_tracker: https://github.com/konstantinvoronov/app_failure/issues
homepage: https://github.com/konstantinvoronov/app_failure

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  meta: ^1.12.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0

topics:
  - error-handling
  - failure
  - clean-architecture
  - result
  - flutter